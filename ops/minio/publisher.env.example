# Publisher host (runs MinIO + replication fanout + site generation)
#
# These variables are read on the publisher host.

# MinIO (S3 API on localhost)
MINIO_ROOT_USER=mspmetro
MINIO_ROOT_PASSWORD=CHANGE_ME

# Optional public URLs if you reverse-proxy MinIO with TLS:
# MINIO_SERVER_URL=https://minio.example.com
# MINIO_BROWSER_REDIRECT_URL=https://minio-console.example.com

# Publisher -> MinIO (use root creds initially)
MINIO_S3_ENDPOINT_URL=http://127.0.0.1:9000
MINIO_S3_BUCKET=mspmetro-site
MINIO_AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
MINIO_AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
MINIO_S3_REGION=us-east-1
MINIO_S3_ADDRESSING_STYLE=path

