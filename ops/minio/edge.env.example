# Edge host (read-only; serves local files)
#
# This repo's current edge implementation uses `cityfeed-puller` over HTTPS and does not require S3 credentials.
# If you switch an edge to S3 sync, use AWS CLI with read-only permissions (List/Get) on the nearest bucket.

# Example (read-only) S3 sync config:
EDGE_NEAREST_ENDPOINT_URL=https://sfo3.digitaloceanspaces.com
EDGE_NEAREST_BUCKET=mspmetro-world

