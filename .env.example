# Backend (Flask + SQLAlchemy)
# Example:
# DATABASE_URL=postgresql+psycopg://mspmetro:mspmetro@127.0.0.1:5432/mspmetro
DATABASE_URL=

# UI (Rust + Dioxus SSR)
BACKEND_ORIGIN=http://127.0.0.1:5000
UI_BIND=127.0.0.1:8080
UI_STATIC_DIR=

# Publisher (S3-compatible origin for cityfeed-puller)
# S3_BUCKET=
# S3_ENDPOINT_URL=https://s3.fr-par.scw.cloud
# S3_REGION=us-east-1
# ORIGIN_BASE_URL=https://pull.s3.fr-par.scw.cloud
# S3_OBJECT_ACL=public-read   # set to "none" to omit ACL
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# IPNS publishing (optional, runs on mspmetro_ipfs hosts during deploy-brief)
# IPNS_KEY_NAME=mspmetro
# IPNS_LIFETIME=168h

# Origin (Scaleway / SCW)
# SCW_ACCESS_KEY=
# SCW_SECRET_KEY=
# SCW_ENDPOINT=https://s3.fr-par.scw.cloud
# S3_BUCKET_GLOBAL=origin-scw
# PUBLISH_ORIGIN_SCW=https://origin-scw.s3.fr-par.scw.cloud
# (legacy name) PUBLISH_ORIGIN_GLOBAL=https://origin-scw.s3.fr-par.scw.cloud
#
# Optional: if edge nodes should pull from a different hostname than the publisher verifies (e.g. a vanity domain or CDN front):
# EDGE_ORIGIN_SCW=https://origin-scw.mspmetro.com

# Origin (DigitalOcean Spaces / DO)
# DO_SPACES_ACCESS_KEY=
# DO_SPACES_SECRET_KEY=
# DO_SPACES_ENDPOINT=https://sfo3.digitaloceanspaces.com
# DO_S3_BUCKET=
# DO_ORIGIN_BASE_URL=https://YOUR_BUCKET.sfo3.digitaloceanspaces.com
# PUBLISH_ORIGIN_DO=https://YOUR_BUCKET.sfo3.digitaloceanspaces.com
#
# If your bucket name contains dots (e.g. edge.origin-do.example), use path-style addressing:
# DO_S3_ADDRESSING_STYLE=path
# DO_ORIGIN_BASE_URL=https://sfo3.digitaloceanspaces.com/edge.origin-do.example
#
# DigitalOcean API (for managing CDN custom domains/certificates)
# DIGITALOCEAN_ACCESS_TOKEN=
#
# Optional: if you put a vanity domain in front of origin-do and want it verified as a website:
# ORIGIN_DO=https://origin-do.mspmetro.com
#
# Optional: if edge nodes should pull from a different hostname than the publisher verifies:
# EDGE_ORIGIN_DO=https://origin-do.mspmetro.com

# Origin (Hetzner Object Storage / HET) or any S3-compatible store (Cloudflare R2 / Backblaze / Wasabi / etc).
# HETZNER_ACCESS_KEY=
# HETZNER_SECRET_KEY=
# HETZNER_S3_BUCKET=
# HETZNER_S3_ENDPOINT_URL=https://<provider-endpoint>
# HETZNER_S3_REGION=us-east-1
# HETZNER_S3_ADDRESSING_STYLE=auto
# HET_ORIGIN_BASE_URL=https://BUCKET.provider.example
#
# MSPMetro convention: origin-het is a tertiary origin for edge nodes. It does not need a public vanity domain.
# Use the provider bucket URL for `HET_ORIGIN_BASE_URL` (e.g. `https://mspmetro-eur.nbg1.your-objectstorage.com`).
#
# Preferred name:
# PUBLISH_ORIGIN_HET=https://mspmetro-eur.nbg1.your-objectstorage.com
#
# Optional: if edge nodes should pull from a different hostname than the publisher verifies:
# EDGE_ORIGIN_HET=https://origin-het.mspmetro.com

# Optional: if you *do* put a real proxy/CDN in front of origin-het and want it verified as a website:
# ORIGIN_HET=https://origin-het.mspmetro.com
