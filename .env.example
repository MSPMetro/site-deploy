# Backend (Flask + SQLAlchemy)
# Example:
# DATABASE_URL=postgresql+psycopg://mspmetro:mspmetro@127.0.0.1:5432/mspmetro
DATABASE_URL=

# UI (Rust + Dioxus SSR)
BACKEND_ORIGIN=http://127.0.0.1:5000
UI_BIND=127.0.0.1:8080
UI_STATIC_DIR=

# Publisher (S3-compatible origin for cityfeed-puller)
# S3_BUCKET=
# S3_ENDPOINT_URL=https://s3.fr-par.scw.cloud
# S3_REGION=us-east-1
# ORIGIN_BASE_URL=https://pull.s3.fr-par.scw.cloud
# S3_OBJECT_ACL=public-read   # set to "none" to omit ACL
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# IPNS publishing (optional, runs on mspmetro_ipfs hosts during deploy-brief)
# IPNS_KEY_NAME=mspmetro
# IPNS_LIFETIME=168h

# Publisher (Scaleway origin example for edge.global)
# SCW_ACCESS_KEY=
# SCW_SECRET_KEY=
# SCW_ENDPOINT=https://s3.fr-par.scw.cloud
# S3_BUCKET_GLOBAL=pull
# PUBLISH_ORIGIN_GLOBAL=https://pull.s3.fr-par.scw.cloud

# Publisher (DigitalOcean Spaces origin example for edge.world)
# DO_SPACES_ACCESS_KEY=
# DO_SPACES_SECRET_KEY=
# DO_SPACES_ENDPOINT=https://sfo3.digitaloceanspaces.com
# S3_BUCKET_WORLD=mspmetro-world
# PUBLISH_ORIGIN_WORLD=https://mspmetro-world.sfo3.digitaloceanspaces.com
#
# If your bucket name contains dots (e.g. edge.world.example), use path-style addressing:
# DO_S3_ADDRESSING_STYLE=path
# DO_ORIGIN_BASE_URL=https://sfo3.digitaloceanspaces.com/edge.world.example
#
# DigitalOcean API (for managing CDN custom domains/certificates)
# DIGITALOCEAN_ACCESS_TOKEN=

# Publisher (third origin / "earth" CDN)
# Configure as any S3-compatible store (Cloudflare R2 / Backblaze / Wasabi / etc).
# EARTH_AWS_ACCESS_KEY_ID=
# EARTH_AWS_SECRET_ACCESS_KEY=
# EARTH_S3_BUCKET=
# EARTH_S3_ENDPOINT_URL=https://<provider-endpoint>
# EARTH_S3_REGION=us-east-1
# EARTH_S3_ADDRESSING_STYLE=auto
# EARTH_ORIGIN_BASE_URL=https://BUCKET.provider.example
#
# Note: a plain CNAME from `earth.mspmetro.com` to a bucket endpoint often breaks HTTPS (cert + Host header).
# Use the provider bucket URL for the puller, or put a real proxy/CDN in front that can rewrite the Host header.

# Optional: public website base to include in `make verify-public` checks.
# ORIGIN_EARTH=https://earth.mspmetro.com
